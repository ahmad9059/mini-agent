{
  "name": "mini-agent",
  "version": "1.0.0",
  "private": true,
  "description": "A small Claude CLI that loads Agent Skills on demand.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/*.js test/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "yaml": "^2.9.0"
  }
}
